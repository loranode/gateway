# GOLANGARCH

Architecture instruction for this repository, generated from `.golangarch.yml` by golangarch-lint.
Follow these rules when creating, moving or editing Go code. Run `golangarch-lint lint .` to verify.
Do not edit this file manually — regenerate it with `golangarch-lint docs`.

## Always enforced (no configuration)

- Every exported function or method must live in a file named exactly after it: `ReadModulePath` -> `read_module_path.go` (normalization: lowercase, underscores removed, `.go` stripped).
- Every unexported function or method must live in a file whose normalized name is a prefix of the function name: `configValidateRule` may live in `config.go`.
- The package name must be exactly equal to its directory name (`package main` is exempt).
- Every directory containing `.go` files must be covered by at least one rule below.
- `*_test.go` files are always ignored by all checks.

## Ignored paths (invisible to all checks)

- `**/*_test.go`
- `api/meshtastic/*.pb.go`
- `api/rest/*.go`

## Rules per path

Rules with a file path (ending in `.go`) override directory rules for that file.

### `cmd/app`

- The directory must stay flat: subdirectories are forbidden.
- Declarations: no types may be declared.
- Declarations: no package-level vars/consts may be declared.
- Declarations: only unexported functions/methods may be declared.
- Imports: stdlib and `github.com/loranode/gateway/internal/config`, `github.com/loranode/gateway/internal/controller`, `github.com/loranode/gateway/internal/worker`, `github.com/loranode/gateway/internal/repositories/**`, `github.com/loranode/gateway/internal/services/**`, `github.com/loranode/gateway/api/rest`, `github.com/merzzzl/proto-rest-api/runtime` only.

### `internal/models`

- The directory must stay flat: subdirectories are forbidden.
- Declarations: only exported types may be declared.
- Declarations: only exported package-level vars/consts may be declared.
- Declarations: no functions/methods may be declared.
- Imports: stdlib only.

### `internal/config`

- The directory must stay flat: subdirectories are forbidden.
- Declarations: no types may be declared.
- Declarations: no package-level vars/consts may be declared.
- Declarations: no functions/methods may be declared.
- Imports: stdlib and `github.com/caarlos0/env/**` only.

### `internal/config/package.go`


### `internal/config/model.go`


### `internal/controller`

- The directory must stay flat: subdirectories are forbidden.
- Declarations: no types may be declared.
- Declarations: no package-level vars/consts may be declared.
- Every function must be a method (declared with a receiver).
- Imports: stdlib and `github.com/loranode/gateway/internal/models`, `github.com/loranode/gateway/internal/services/**`, `github.com/loranode/gateway/internal/mappers`, `github.com/loranode/gateway/api/rest` only.

### `internal/controller/package.go`

- Declarations: only unexported types may be declared, except names matching `Controller`.
- Declarations: only unexported package-level vars/consts may be declared.
- Declarations: only unexported functions/methods may be declared, except names matching `New*`.
- Imports: stdlib and `github.com/loranode/gateway/api/rest`, `github.com/loranode/gateway/internal/services/**` only.

### `internal/worker`

- The directory must stay flat: subdirectories are forbidden.
- Declarations: no types may be declared.
- Declarations: no package-level vars/consts may be declared.
- Every function must be a method (declared with a receiver).
- Imports: stdlib and `github.com/loranode/gateway/internal/models`, `github.com/loranode/gateway/internal/services/**`, `github.com/loranode/gateway/api/meshtastic`, `google.golang.org/protobuf/**` only.

### `internal/worker/package.go`

- Declarations: only unexported types may be declared, except names matching `Worker`.
- Declarations: only unexported package-level vars/consts may be declared.
- Declarations: only unexported functions/methods may be declared, except names matching `New*`.
- Imports: stdlib and `github.com/loranode/gateway/internal/services/**` only.

### `internal/repositories`

- The directory must contain only subdirectories: files are forbidden.

### `internal/repositories/*`

- The directory must stay flat: subdirectories are forbidden.
- Declarations: no types may be declared.
- Declarations: no package-level vars/consts may be declared.
- Every function must be a method (declared with a receiver).
- Exported function parameters: stdlib types and types from `github.com/loranode/gateway/internal/models` only.
- Exported function results: stdlib types and types from `github.com/loranode/gateway/internal/models` only.
- Imports: stdlib and `github.com/loranode/gateway/internal/models`, `github.com/loranode/gateway/migrations`, `modernc.org/sqlite`, `github.com/pressly/goose/**` only.

### `internal/repositories/*/package.go`

- Declarations: only unexported types may be declared, except names matching `Repository`.
- Declarations: only unexported package-level vars/consts may be declared, except names matching `Err*`.
- Declarations: only unexported functions/methods may be declared, except names matching `New`.
- Imports: stdlib only.

### `internal/services`

- The directory must contain only subdirectories: files are forbidden.

### `internal/services/*`

- The directory must stay flat: subdirectories are forbidden.
- Declarations: no types may be declared.
- Declarations: no package-level vars/consts may be declared.
- Every function must be a method (declared with a receiver).
- Exported function parameters: stdlib types and types from `github.com/loranode/gateway/internal/models` only.
- Exported function results: stdlib types and types from `github.com/loranode/gateway/internal/models` only.
- Imports: stdlib and `github.com/loranode/gateway/internal/models`, `github.com/loranode/gateway/internal/repositories/**` only.

### `internal/services/*/package.go`

- Declarations: only unexported types may be declared, except names matching `Service`.
- Declarations: only unexported package-level vars/consts may be declared, except names matching `Err*`.
- Declarations: only unexported functions/methods may be declared, except names matching `New`.
- Imports: stdlib and `github.com/loranode/gateway/internal/repositories/**` only.

### `internal/mappers`

- The directory must stay flat: subdirectories are forbidden.
- Declarations: no types may be declared.
- Declarations: no package-level vars/consts may be declared.
- Declarations: only exported functions/methods may be declared.
- Imports: stdlib and `github.com/loranode/gateway/internal/models`, `github.com/loranode/gateway/api/rest` only.

### `migrations`

- The directory must stay flat: subdirectories are forbidden.
- Declarations: no types may be declared.
- Declarations: only exported package-level vars/consts may be declared.
- Declarations: no functions/methods may be declared.
- Imports: stdlib only.
