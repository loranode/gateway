# TCP address of the Meshtastic node's client API (WiFi enabled, port 4403).
# A bare host gets :4403 appended automatically.
NODE_ADDR=meshtastic.local:4403

# Listen address of the REST server.
HTTP_ADDR=:8080

# SQLite database file path.
DB_PATH=sqlite.db
